# Public SCSS

一套供多个前端项目共享的基础样式与原子工具类库，以 SCSS 变量和生成器为核心。

## Language

**工具类**:
一个只做一件事、可直接写在 HTML 上的原子 class。
_Avoid_: 公共 class、辅助类、helper

**刻度类**:
沿一条数值刻度批量生成的一族工具类，类名以简写前缀加数值构成。间距、字号、字间距、尺寸都属于这一族。
_Avoid_: 数值类、批量类

**语义类**:
描述一种具名状态或意图、不带数值的单例工具类，类名用完整单词拼写。
_Avoid_: 具名类、固定类

**尺寸**:
元素自身的宽与高。与元素之间的**间距**是两个独立的刻度族。
_Avoid_: 大小、size、尺度
